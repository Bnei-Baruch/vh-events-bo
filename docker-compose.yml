services:
  vh-events-bo:
    image: "${IMAGE_NAME}"
    restart: unless-stopped
    cpus: '0.1'
    mem_limit: 50M
    ports:
      - 8089:80
    volumes:
      - /opt/vh/vh-events-bo/public/config/:/usr/share/nginx/html/config/
networks:
  default:
    name: vh
    external: true
